;;; Package --- This is base config for my Emacs
;;; Commentary:

;;; Code:
;;(define-key helm-gtags-mode-map (kbd "M-s f") 	'find-grep)
(global-set-key (kbd "M-s f") 'find-grep)

(provide 'config-base)
;;;
